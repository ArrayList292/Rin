name: Node.js CI
on: [push, pull_request]
jobs:
  build:
    runs-on: windows-latest
    strategy:
      matrix:
        node-version: [12.x]
    steps:
      - uses: actions/checkout@v2
      - name: Use Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v1
        with:
          node-version: ${{ matrix.node-version }}
      - run: cd src
      - run: npm i -g eslint
      - run: npm i babel-eslint --save-dev
      - run: eslint -c package.json --max-warnings 1
        env:
          CI: true